<h1>Send Text</h1>

<%= form_tag 'send_text/send_text_message' do %>
	<p>
		<%= label_tag(:number_to_send_to, "Send to:") %>
		<%= text_field_tag(:number_to_send_to) %> <br />
		<%= text_area_tag(:message) %>
		<%= submit_tag 'Send' %>
	</p>
<% end %>